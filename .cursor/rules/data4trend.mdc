---
alwaysApply: true
---

- 必须启用代理，不允许禁用代理
- 数据库必须使用docker已经启动的clickhouse
- 不允许胡乱创建文件，如果相同功能基于原先文件改- 必须启用代理，不允许禁用代理
- 数据库必须使用docker已经启动的clickhouse
- 不允许胡乱创建文件，如果相同功能基于原先文件改
- 不允许硬编码要获取1m数据的代币，比如是币安全部的代币
- 不允许禁用代理连接
  